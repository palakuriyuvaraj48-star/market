spring:
  jpa:
    hibernate:
      ddl-auto: validate # Database schema migrations handled externally in production
    show-sql: false
  logging:
    level:
      root: INFO
      org.springframework.security: WARN
      org.hibernate.SQL: WARN
      com.freshmartpro: INFO
